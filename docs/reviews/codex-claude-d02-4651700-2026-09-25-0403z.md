# Claude D02 TXT 解析器审查

- 审查时间：2026-09-25 04:03 UTC；目标 worktree：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/agent-a6e6226be56d04b53`。
- 固定范围：`588d00a..465170080d4c4381b6342900750dbdfe50b84255`；差异 SHA-256 `75d4844a540485f40706a1d93d2d53b342960f5832c7427be081ce2529aa37a6`。覆盖 `docs/handoffs/claude-d02.md`、`src/backend/app/services/parsers/txt.py`、`tests/backend/test_d02.py`；这三文件无待审余项。不覆盖与其他解析器的集成或其他 worktree。
- 完成门禁：交接标记 `ready_for_review`，记录基线 `588d00a`；目标 HEAD 为上述固定提交且 worktree 清洁。04:01:55 与 04:02:15 UTC 两次观察的 HEAD、dirty diff SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`、交接 SHA-256 `60bc9a651be51fc451c494c4c9ecdb90d8370392eb8aceea97a3b3ed83b3373a` 一致。本项目 `~/.claude/projects/` 下没有该子 worktree 的会话目录；本次依据固定交接与稳定 git 状态进入审查，未读取其他项目会话。

## 问题

### D02-R01 — P2：无可见字符的控制字节被当作有效 TXT

- **目标提交与位置**：`465170080d4c4381b6342900750dbdfe50b84255`，`src/backend/app/services/parsers/txt.py:134-135,233-241`。
- **触发条件**：上传不含 NUL、但只含其他 C0 控制字节的文件，例如 `b"\x01\x02"`。独立复现得到 `ParsedDocument`，正文为 `"\x01\x02"`、来源为 `第1段`。`b"%PDF-1.7\n\x01\x02"` 也被作为两行 TXT 正文接受。
- **影响**：没有可见文本或扩展名伪装的二进制内容会被标记为解析成功，并进入后续分块/来源链；与 D01 的“没有可提取文本则报 `no_text`”及 D02 交接中的二进制损坏语义不符。现有 94 个测试只覆盖含 NUL 的二进制，没有覆盖无 NUL 控制字节。
- **最小修复建议**：在解码后统一校验非文本控制字符（保留明确允许的换行、制表等）；只有不可见控制字符时抛 `DocumentUnreadableError(no_text)`，掺在内容中的二进制控制字符按 `corrupted` 处理。新增上述两个字节串的失败用例，保持合法空白、Ctrl-Z 及 UTF-8/GBK 回归用例通过。

## 验证与边界

- 先检查 `test_d02.py`、解析器及 `verify.sh`；随后以 `PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=<目标>/src/backend /opt/anaconda3/bin/python -m pytest -p no:cacheprovider tests/backend/test_d02.py -q` 运行：**94 passed，退出码 0**。未安装依赖，也未在目标 worktree 写入缓存。
- 独立运行上述两组反例均返回 `ParsedDocument`；`git diff 588d00a..4651700 --check` 退出码 0；测试后目标 HEAD、清洁状态和交接指纹未变。
- 未运行整套 `verify.sh` 或后端全量测试：本次固定差异仅 TXT 解析器；系统 Python 缺 pytest，使用现有 Anaconda Python 完成定向测试。课程权限、发布版本、DAG、取消/重试、双存储及问答不在此批次代码范围内，不据此评价那些模块或整轮集成。
