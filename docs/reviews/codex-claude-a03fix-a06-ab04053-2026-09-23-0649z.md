# Claude 审查批次：A03 修复与 A06 租约规范

## 固定范围与完成判定

- 目标 worktree：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a03-d430b9`。A03 修复为 `2049129..6345ce1`；A06 为 `6345ce1..ab04053413540c18f480ca90fc1162d1efd21f45`。两次提交都仅改文档与交接，没有运行时代码。
- 本项目会话 `254b679d-7d12-4fdc-8319-ed1765633ce9` 的最后有效项是 2026-09-23T06:45:55.691Z `stop_hook_summary`；其后无 user/tool 活动，完整读取游标为 2954230。A06 交接标记 `ready_for_review`，固定提交为 `ab04053`。
- 06:48～06:49Z 两次观察均为同一 HEAD、空 dirty diff（SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`）、A06 交接 SHA-256 `bda89c07ceb7c16efd4db885373c95033b77694538fc24b2da8b38c136dc0fdb`、`2049129..HEAD` 二进制 diff SHA-256 `62c5b460477c3bf4ab4fdcab80688be0d75f13bac8ee85ef5335ccb0bac35492`。A06 自身四文档 diff SHA-256 `09bc765c49e32934bf6fea801794530a368896094c8ef7ca9c054beddc75675c`，与交接的前 16 位一致。
- 覆盖 A03 修复的六个改动文件，以及 A06 的 `docs/architecture.md`、`docs/decisions.md`、`docs/tasks.md`、`specs/task-processing.md`、`docs/handoffs/claude-a06.md`。A03-R01/R02 的 SSE 关流与 T6 任务水位措辞已对齐规格、ADR、架构及课程验收；下述新问题只针对 A06。未重报已审基线或其他 worktree 的旧问题。

## 新问题

### A06-R01 P1：失败清理会删除被其他任务复用的图元素

- **目标提交 / 位置**：`ab04053`，`specs/task-processing.md:279-282`；同一决定见 `docs/decisions.md:284`。
- **触发条件**：任务 A 在 Neo4j 中新建关系并提交，但在 SQLite T6 前崩溃；任务 B 随后按「课程 + 类型 + 起点 + 终点」的同一关系 ID `MERGE` 该关系，并加入自己的来源关联。A 多次接管仍失败，执行按 `created_by_task = A` 的清理。
- **影响**：关系的创建标记仍为 A，按该标记撤销整条关系会连 B 正常提交的关系及其来源一起删掉；若 B 复用了 A 新建的节点，同样可能删掉 B 的节点/边。课程锁只串行写入，不阻止写入后的复用。违反跨任务隔离及失败清理只撤销本任务贡献的意图。
- **最小修复建议**：把来源/贡献记录为按任务分离的关联；清理先移除 A 的贡献，再仅删除没有其他任务或人工引用的节点/边。增加 A 写入后崩溃、B 复用并 T6、A 最终失败的回归，断言 B 的关系与来源仍在。

### A06-R02 P1：`cleanup_pending` 期间失败任务仍可暴露草稿

- **目标提交 / 位置**：`ab04053`，`specs/task-processing.md:280-282`；与 `specs/task-processing.md:89` 的 I6 相冲突。
- **触发条件**：任务已把 Neo4j 草稿提交，随后 T6 或清理发生存储故障；规范允许先执行 T9，令 `stage = failed`、`cleanup_pending = true`，等回收者稍后删除。Neo4j 恢复后、清理运行前教师读取草稿。
- **影响**：任务已为 `failed`，但其节点/边仍对教师可见；发布/审核也可能在这个窗口纳入失败任务内容。I6 明确要求失败任务不得留下教师可见的草稿。周期回收与课程锁均未给读路径或发布路径设置可见性门禁。
- **最小修复建议**：在清理完成前让未 T6 提交或 `cleanup_pending` 的任务内容对草稿查询、审核与发布不可见，或先写隔离暂存图并在 T6 后原子提升；补 Neo4j 故障→T9→恢复但尚未清理的读取和发布回归。

## 验证、范围与下一步

- 已先阅读 `scripts/verify.sh` 与其 hook 测试；在目标 worktree 执行 `./scripts/verify.sh` exit 0（输出 `block-dangerous hook tests passed.`、`Scaffold verification passed.`），`git diff 2049129..HEAD --check` exit 0。该脚本仅检查骨架/hook，不执行 LEASE-1～17。Claude 交接中的 45 项文字核对、9 个负例及 A03 文字核对脚本均未作为仓库测试入库；本批没有真实 SQLite/Neo4j 或 worker 实现可运行。
- A03 `6345ce1` 的前轮两项 P2 文字冲突在固定范围内已修；它尚不代表 B10/G04 的运行时行为已实现或验证。A06 是设计文档审查完成、**带阻塞问题**，不是 worker 机制通过。S-07 合并/ADR 范围和其他旧分支 pending 仍按状态文件保留。
- 请先修订 A06 的失败清理与失败态可见性协议，再让 F13/G04/C09 消费；修复应另以稳定提交和交接进入下一轮。未修改 Claude worktree，未安装依赖、调用模型、提交、合并或推送。
