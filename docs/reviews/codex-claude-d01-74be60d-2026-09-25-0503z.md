# Claude D01 解析输出模型审查

- 时间：2026-09-25 05:03 UTC。目标 worktree：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/agent-aec40eaf0087d513d`，固定提交 `74be60de24f797796bfe648ccd288c91be75a0b0`。
- 审查基线：D01 初版 `68affa8..3b38724` 与修复 `83e5338..74be60d`；用合并后公共基线 `909ce33..74be60d` 核对本轮 10 个文件，差异 SHA-256 `794cb6a34f97d79e13eae31d224163c0fe5420564f3b9186d5defa5ac9182081`。中间 `83e5338` 为主线合并，不将 C05/C08/E01 实现算作 D01 审查。
- 完成信号：`docs/handoffs/claude-d01.md` 标记 `ready_for_review`，绑定固定提交。04:58 与 05:02 UTC 两次 HEAD、清洁 worktree（dirty SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`）、交接 SHA-256 `9ab7e2ac4fd6ecfd4a07e803613cb53b02a4b4dd599a433539447d501537d98b`、差异指纹相同。项目会话目录中无此子 worktree 的匹配目录；因此按固定交接审查，未以 `stop_hook_summary` 冒充完成。

## 问题

### D01-R01 — P3：修订哈希校验接受末尾换行

- **目标提交/位置**：`74be60d`，`src/backend/app/services/parsers/models.py:57,295-297`。
- **触发条件**：`RevisionKey("doc_01", "sha256:" + "a" * 64 + "\n", "txt/1")` 被接受；Python 正则 `$` 可匹配最终换行之前，`re.match` 因而未覆盖整串。独立运行复现，返回的 `content_hash` 保留 `\n`。
- **影响**：`RevisionKey` 宣称的 `sha256:<64 个小写十六进制>` 不变量不成立；若 D11 或其他调用方传入带末尾换行的摘要，资料修订身份可能与 C05 的规范摘要分叉，造成重复修订或关联失败。当前 C05 正常生成的摘要不带换行，故限为低优先级边界缺陷。
- **最小修复**：改用 `_HASH_RE.fullmatch(self.content_hash)`（或 `\Z` 锚定），增加末尾 `\n`、`\r\n` 的拒绝测试，同时保留 C05 `StoredFile.content_hash` 的正例。

## 验证与范围

- 已读目标 AGENTS、后端/测试角色规则、D01 原子任务、`teacher-review-publish` / `task-processing` 相关规格、架构定位规则、交接、模型、导出、88 项测试和自编 fixture；检查 SourceRef 的 `page`/`section_path` 约束。覆盖 D01 的 10 个差异文件；无本批待审文件。
- `PYTHONPATH=<目标>/src/backend /opt/anaconda3/bin/python -m pytest tests/backend/test_d01.py -q`：**88 passed，exit 0**。哈希末尾换行独立反例被接受。`git diff 909ce33..74be60d --check`：exit 0。
- 原样 `./scripts/verify.sh`：exit 1，系统 Python 缺 PyYAML、OpenAPI validator 与 pytest；使用**已有** Anaconda Python 重跑 `PATH=/opt/anaconda3/bin:$PATH ./scripts/verify.sh`：exit 0，24 个契约门禁负例及 B08/B09/B10/B13 回归通过。未安装依赖、调用付费模型或修改 Claude 实现。
- 本批仅审 D01 解析值对象与 fixture；课程权限、发布隔离、DAG 并发、任务取消/重试、双存储与问答运行时不在本次差异中。D02、D03～D05、C05、集成 worktree 等仍分别待审或已有独立报告，不据此宣称整轮通过。
