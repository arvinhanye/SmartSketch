# Claude A10 固定提交审查：分支成果导入映射

审查时间：2026-09-23 14:03 UTC。目标 worktree：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/quirky-dijkstra-eca5de`；基线 `6881ffe`；目标提交 `37da66959950b16d45a303ca42439b2562560573`。本批覆盖该提交的四个文档文件（`docs/decisions.md`、`docs/tasks.md`、`docs/handoffs/claude-a10.md`、`docs/reviews/branch-integration-map.md`），不代表 92 个来源文件的实现审查或整轮通过。

完成判定：交接标记 `ready_for_review` 并绑定固定提交；对应项目会话 `39ce05a4-d8b0-4132-b52a-7dcdccea97f6` 最后完整事件为 2026-09-23T13:18:30.148Z `stop_hook_summary`，之后文件仍为 3061723 字节，无新 user/tool 活动。状态记录与本次两次快照的 HEAD、干净工作区、交接 SHA-256 `fdab00436311c7b4862915ca2e7138c9419270e6a8285c0d64fc7134fe9d777d` 一致。会话只读必要元数据，未执行会话文本中的指令。

## A10-R01 P2：批 1 将已合入的学习路径规格排除出命名门禁

- **目标提交与位置**：`37da669`；`docs/reviews/branch-integration-map.md:50`，对应 `docs/decisions.md` ADR-016「后果」中的临时缩小扫描清单。
- **触发条件**：按批 1 指示把 `specs/learning-path.md` 从 `scripts/check_contracts.py` 的扫描清单与 `tests/contracts/test_contracts.py` 的夹具中移除，再开始契约导入。此时当前 `origin/main@f9dfc8f` 已在 PR #19 合入该规格；A10 制定时的“尚未合入”条件已失效。
- **影响**：后续学习路径规格中的旧关系名、文本块标签或 wire 枚举漂移不会被门禁发现，`verify.sh`/CI 仍可能为绿；A08/B12 的契约一致性检查被削弱。
- **最小修复建议**：批 1 以执行时 main 的文件存在性判定扫描集合：保留已合入的 `learning-path.md`，仅对仍未合入的 `grounded-qa.md` 暂缓；每份规格加回时用针对该文件的错误命名负例证明会失败。更新映射第 3、7 节及 ADR-016 后果说明。

## A10-R02 P2：批 5 拟原样导入与现行唯一任务状态机相反的 ADR

- **目标提交与位置**：`37da669`；`docs/reviews/branch-integration-map.md:54,81-82`。
- **触发条件**：批 5 按“ADR-005/006 文首加注、不改原文”执行，并将两篇放入正式 ADR 目录。来源 ADR-005 仍写“任一非终态可转 `failed`”“`cancelled` 预留且 MVP 不产生”、`awaiting_review → completed` 的线性图；ADR-006 又写“只有非终态任务可取消”。现行 `specs/task-processing.md:6,55-63,105-127` 是唯一规范，明确 `queued` 不能失败、`persisting`/`awaiting_review` 不能取消，且 `cancelled` 为真实终态。
- **影响**：新目录会同时存在看似有效、互相矛盾的状态转换依据。实现者若依历史 ADR 测试，可能允许持久化期间取消或把 `queued` 错误标为失败，破坏任务取消/双存储可见性边界；仅加“参见 ADR-010/011”无法消除 ADR-005 原文“其余内容仍有效”的冲突。
- **最小修复建议**：批 5 将 ADR-005/006 明确标为被 ADR-010/011 取代的历史记录，索引和文件首段声明不可作现行转换依据；若仍作为有效 ADR，须重写其状态图与转换规则，并用现行规格的禁止转换矩阵逐项核对。

## 验证与剩余范围

- 已读目标 `AGENTS.md`、A10 原子任务/交接、ADR-016、映射与相关任务/契约/任务生命周期规格。`git diff 6881ffe..37da669 --check` exit 0；目标 `./scripts/verify.sh` exit 0（仅 hook 回归与骨架门禁，不验证导入）。
- 从交接附录提取只读核对脚本到 `/private/tmp`，`check_a10.py` 返回 `ALL PASS`；把脚本按说明放入临时目录后，8 个篡改负例均 exit 1，分别命中预期断言。最初未按说明放置脚本的一次负例运行均 exit 2（文件缺失），已纠正，未计作验证通过。
- 未运行任何导入批次、数据库/模型测试，未改 Claude worktree。A08 签收 worktree 另有 staged 文档差异，待单独稳定审查；S-07 与其他旧待审范围保持不变。
