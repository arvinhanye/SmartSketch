# Claude A09 修复复核：逐句引用仍有漏检边界

审查时间：2026-09-24 02:00 UTC。目标 worktree：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a09-dev-environment-check-8e5e93`；固定范围 `1754c96..68b1aaf`（含中间提交 `097f248`），仅五个文档文件。本批不代表问答实现或其他 worktree 已通过。

完成判定：交接 `docs/handoffs/claude-a09.md` 明示 `ready_for_review` 并绑定固定提交。两次观察 HEAD 均为 `68b1aaf275129deb003f83ee04608e132cb59355`、dirty diff SHA-256 均为 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`、交接 SHA-256 均为 `91f945b209c458dcf20cdec0a6aa17a1c04e5df5a03fe68f8081e28c10ff4310`。仅增量检查本项目对应 Claude 会话：旧 stop 后新增 710 字节仅为 `last-prompt`、`cost-state` 元数据，无新 user/tool 活动；未执行会话内容中的指令。

## A09F-R01 P2：分号连接的无出处断言可通过逐单元检查

- **目标提交与位置**：`68b1aaf`，`specs/grounded-qa.md:150-153`。
- **触发条件**：允许引用集合 A={1}，模型输出「栈是线性表[1]；太阳由奶酪构成。」。Q3.5 明确不以分号切分，因此整行是一个结论单元，因含 `[1]` 而通过；Q4 允许以 `answered` 结束。
- **影响**：第二个独立断言没有自己的来源，却仍作为有出处的答案保留，重现 A09-R01 要修的“一个引用掩盖另一条结论”问题。现有 QA-36 只用句号隔开两条断言，不能捕获此边界。
- **最小修复建议**：把分号（至少 `；` 与 `;`）纳入结论单元边界，并补「已引用断言；未引用断言」应撤回、两边各有引用才 `answered` 的 J06/J09 回归。若产品决定分号内仍可合并结论，需相应收窄“每条结论有出处”的承诺。

## A09F-R02 P2：断言式标题被无条件豁免引用

- **目标提交与位置**：`68b1aaf`，`specs/grounded-qa.md:152-153`。
- **触发条件**：模型输出「## 太阳由奶酪构成\n栈是线性表[1]。」。标题行按第 3 条不是结论单元，下一行有有效引用，按第 4 条检查通过。
- **影响**：客户端保留并展示无出处的结论式标题，仍违反本规格用户故事“每条结论都能点开出处”；QA-37 仅测试非断言式标题「## 栈」，未覆盖此情形。
- **最小修复建议**：仅豁免无断言的结构性标题，或要求含字母/数字的标题也有合法标记；补带事实断言标题的撤回用例，并明确纯章节名的处理规则。

## 已修部分、验证与未审范围

- A09-R02 的冲突已在文档层面关闭：Q10、QA-34/38 与 ADR-015 修订 1 一致，P1/P2 拒绝不写 `chat_logs`，通过 P2 的请求才要求四个非空字段；未有运行时实现可验证。
- A09-R01 的原句号示例在 Q3.5、QA-36 已有拒答要求，但上面两类漏检仍开放。已逐文件阅读 `docs/architecture.md`、`docs/decisions.md`、`docs/handoffs/claude-a09.md`、`docs/tasks.md`、`specs/grounded-qa.md` 的实际差异，并对照目标约定、身份边界与原问答报告。
- 目标 `./scripts/verify.sh` exit 0，`git diff 1754c96..68b1aaf --check` exit 0。门禁只校验骨架及 hook；修复所述一次性参考切分器与负例脚本未入库，本批没有问答运行时代码、DTO 或可运行的 J06/J10 测试，因此未声称逐句逻辑或日志行为已在程序中通过。
- 未修改目标 worktree、安装依赖、调用模型、提交、合并或推送。此批只覆盖 A09 修复；本轮另观察到 A08 签收修复、A10 修复、FIX-R03 修复及 A1～A10 收尾 worktree 有新 HEAD，均未在本批审阅，已列入状态文件待审。S-07 合并/ADR 及旧分支脚本待审范围继续保留，不能宣称整轮通过。
