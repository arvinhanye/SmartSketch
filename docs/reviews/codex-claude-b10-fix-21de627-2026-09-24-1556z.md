# Claude B10 修正提交审查（2026-09-24 15:56 UTC）

## 固定范围与结论

- 目标：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a05-aa1561` 可达的固定提交 `21de62767e303b6477808e138c6d48e7d1d87b06`；基线 `3771ae1f576b88bfe6f4798a17b00fa32e3179b1`。只审 REVIEW-B10 R01～R04 修正的 11 个文件，不把后续合并、B13 或父分支集成算入本批。
- 固定差异 SHA-256：`fd4525b7258ad4ff062aa4574deb571f4284e315d52e54c06af2d3f5bb89e504`。交接 `claude-review-b10.md` SHA-256：`e020a4a089ec5e2c8165f297f96d2227a0a76f067f3f4d733f9037ca4ec1fb96`。
- `ready_for_review` 交接绑定上述提交；父 worktree 在两次观察中 HEAD `05c5c03`、空 tracked diff 与交接指纹一致。匹配的 SmartSketch 会话在该提交后有新 user 事件，因此不把旧 stop 标记当作整会话结束；本批仅按固定提交进入审查。
- 原 B10-R01/R02 在 JSON Schema 层已关闭：错误的 `awaiting_review` 进度及 `cancelled + false` 均被拒。新发现 **1 项 P2、1 项 P3**；不能据此宣布整轮或集成分支通过。

## 问题

### B10F-R01 — P2：生成的取消快照模型接受缺失的必填标志

- **目标提交/位置**：`21de627`，`src/contracts/api.v1.yaml:1393-1404`；生成物 `src/contracts/v1/generated/python/models.py:196-199`。
- **触发条件**：`Task.model_validate` 接收 `stage='cancelled'`、但没有 `cancel_requested` 的快照；`TaskCancelled` 把继承的必填字段覆盖成带默认值的 `Literal[True] = True`。同一载荷由 OpenAPI JSON Schema 拒绝。以 `model_dump(exclude_unset=True)` 序列化后仍不含该字段。隔离副本实测为 `jsonschema=False, pydantic=True`。
- **影响**：如果 C10/C11 通过入库的生成模型验证并序列化取消响应，可能发出违反 `TaskBase.required` 与 §4「200 快照必须含真实标志」的载荷；H02 无法凭响应显示取消结果。现有生成模型测试只测 `false`，没有测缺字段。
- **最小修复**：调整真源/生成方式，令生成的 `TaskCancelled.cancel_requested` 无默认且只能为 `true`，或在服务端响应校验层明确使用 JSON Schema 并拒绝缺字段；新增对 `Task.model_validate` 缺字段及 `exclude_unset=True` 的回归。

### B10F-R02 — P3：409 详情对象未真正封闭

- **目标提交/位置**：`21de627`，`src/contracts/api.v1.yaml:1445-1476`（`TaskNotCancellableError.details` 的三个 `oneOf` 分支）。
- **触发条件**：`details={stage: completed, reason: already_terminal, secret: x}` 通过 JSON Schema；各分支未设置 `additionalProperties: false`。隔离副本实测 `jsonschema=True`，生成 Pydantic 模型则静默丢掉 `secret`。
- **影响**：交接与 schema 描述声称详情为闭集，但 wire 校验允许附加字段，生成模型又会改变它；取消错误的日志/客户端形状不一致，附加内部数据也可能被误带入响应。
- **最小修复**：三个 `details` 分支均加 `additionalProperties: false`，增加附加字段负例，并确认生成模型的额外字段策略与 wire 约束一致。

## 验证与未审范围

- 在 `/private/tmp/smartsketch-b10fix.i1hRq4` 用 `git archive 21de627` 建立隔离副本；未改 Claude worktree。
- `/opt/anaconda3/bin/python -m pytest tests/contracts/test_b10.py -q`：**45 passed**。独立 `jsonschema` 与生成 Pydantic 反例执行结果如上；`git diff 21de627^ 21de627 --check` 通过。
- `PATH=/opt/anaconda3/bin:$PATH ./scripts/verify.sh` 的隔离副本日志出现「生成物与真源一致」、22 项负例通过、B08/B09 各 5 项通过、B10 45 项通过及最终 `Scaffold verification passed.`；命令工具在收尾时被中断，未取得可靠退出码，故记录为**输出完整但退出码未确认**，不冒充已确认 exit 0。
- 本批只审契约、生成 DTO、相关规格/测试与交接；票据核销、权限、任务状态机与双存储运行时尚未在 B10 实现。`bb48429` 的 main 集成差异、CI-02、B13 和旧待审范围继续排队。
