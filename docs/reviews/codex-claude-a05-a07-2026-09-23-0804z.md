# Claude 审查批次：A05 身份边界与 A07 模型配置

## 固定范围与完成判定

- A05：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a05-aa1561`，会话 `187773cd-02bd-4ec5-9119-eed82ace5882`，最后停止事件 `2026-09-23T07:30:16.267Z`；固定差异 `931361d..e0b7ccd34b8ae4ba8c06654e2a165d2ac047ccab`（4 文件），交接 `docs/handoffs/claude-a05.md` 标记 `ready_for_review`。
- A07：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a07-297f68`，会话 `70fe9db6-2403-47ef-abb6-29352e417c20`，最后停止事件 `2026-09-23T07:28:35.370Z`；固定差异 `ab04053..af9ff7d4ceeed23da97495819e4a5b27e54b9e94`（4 文件），交接 `docs/handoffs/claude-a07.md` 标记 `ready_for_review`。
- 上轮与本轮两次观察中，两会话均无停止后的新字节；两个 HEAD 均未变化，staged、unstaged、untracked 均为空（dirty SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`）。交接所在提交固定且工作树干净。A05/A07 的交付均为文档，没有运行时代码。
- 本批逐文件审阅两份提交的实际差异，并对照各自 AGENTS/CLAUDE 约定、任务、身份规格、A03/A06 任务处理规格及已在前批审过的契约基线。A03/A06/A04 的已报问题不重复报。A04 worktree 自上轮后已有新 user/tool 活动与新 HEAD，本批不把它视作稳定完成。

## 新问题

### A07-R01 P2：`model_calls` 去重键未明确覆盖物理重试与问答调用

- **目标提交 / 位置**：`af9ff7d4ceeed23da97495819e4a5b27e54b9e94`，`docs/integrations.md:138-141`；其依赖规范 `specs/task-processing.md:284`。
- **触发条件**：同一任务、块、用途、块级尝试序号内发生两次可计费物理调用，例如主用请求已在供应商侧产生 usage、客户端断线后按 L1 重试，或输出不合规后按 E05 再调用修复。A07 要求重试、备用及修复调用都计入预算，但沿用的 ADR-011 键仅为「任务 + 块 + 用途 + 尝试序号」，没有区分物理调用的序号或 ID。
- **影响**：若「尝试序号」只指块级或任务级尝试，按该键 `UPSERT`/去重会吞掉另一次实际调用的 usage，使任务与每日预算、审计账本低估；若它指物理调用，规范也未写明稳定生成及重放语义。问答调用没有任务 ID，当前键形状同样未定义全站每日问答预算的去重身份。结构核对未覆盖这些语义，尚无运行时实现可验证。
- **最小修复建议**：把去重标识改为每次**物理供应商调用**的稳定 `call_id`（或含任务/块/用途、L3/L2/L1 序号、供应商及调用序号），重放同一调用仍只计一次，真正重试与备用调用各计一次；问答用独立请求 ID 且允许无任务 ID。同步修订 A06 §8.4、ADR-011 与 A07 预算说明。补「主用计费失败→重试/备用成功→重放写账」及无任务 ID 问答的预算回归，断言所有实际 usage 入账一次。

## 无新增问题的范围与验证缺口

- A05 的 `specs/identity-access.md`、ADR-013、任务板及交接，本批未发现新的内部矛盾；已写明的成员 API/登录/票据承接任务缺口仍是实施前置，不把它误记为已实现。SSE 一次性票据的断线重申领规则在 §5.2 已覆盖。
- A07 的 `.env.example`、`docs/integrations.md`、任务板及交接已审；D-02a～f 真实取值仍未签收，样例不是生产选型。A07-R01 应在 E04/C01 落实预算持久化前澄清。
- `git diff 931361d e0b7ccd --check`、`git diff ab04053 af9ff7d --check` 均 exit 0。读过 `scripts/verify.sh` 与 hook 回归脚本后，在各目标 worktree 运行 `./scripts/verify.sh` 均 exit 0（仅骨架与 hook，不证明身份/预算语义）。未安装依赖、调用付费模型或修改 Claude worktree；没有运行时服务、数据库/并发或问答引用测试可执行。本批完成两份固定文档差异的审查，结论不是整项目通过。
