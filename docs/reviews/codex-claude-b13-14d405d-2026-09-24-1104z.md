# Claude B13 固定提交审查（2026-09-24 11:04 UTC）

## 范围与完成信号

- 目标：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a09-dev-environment-check-8e5e93`；`3771ae1f576b88bfe6f4798a17b00fa32e3179b1..14d405d1907bf8d0f11977fb4cd759a312289324`（B13，不重复 B10）。交接 `docs/handoffs/claude-b13.md` 标为 `ready_for_review` 且绑定此提交。
- 14 个变更路径的实际 diff 均纳入本批，包括真源、事件文档、生成物、门禁和测试；仅评 B13 契约，不评整个父分支或后续 B11～B14 实现。未跟踪的 `src/backend/smartsketch_backend.egg-info/` 不属于固定提交，未纳入。
- 本项目匹配 worktree 的 Claude 会话 `52c4c9a5-9112-49bd-b0be-3b3ad8cedf78` 末次 `stop_hook_summary` 为 2026-09-24 06:57:33.826Z，其后仅元数据，无 user/tool 活动；不把 stop 本身当成功。HEAD、tracked dirty diff、未跟踪文件列表与交接 SHA-256 在两次观察中相同（HEAD `14d405d`，dirty diff 空，交接 `622eaad8df71…`）。

## 问题

### B13-R01 — P2：生成 DTO 未兑现条件必填约束

- **目标提交/位置**：`14d405d`；`src/contracts/api.v1.yaml:2164-2172,2225-2248`；`src/contracts/v1/generated/python/models.py:617-640,653-659,761-767`；`src/contracts/v1/generated/typescript/openapi.d.ts:1158-1164`。
- **触发**：使用生成的 Python `ChatErrorEvent.model_validate` 校验 `LLM_UNAVAILABLE` 且 `details` 只有 `request_id`、没有 `reason`；或校验 `status=not_covered`、`retrieved=2` 的 `ChatMetaEvent`。两者均通过，尽管独立 JSON Schema 拒绝。TS 的 `ChatError.details.reason` 也生成为可选。隔离副本中实际运行两个反例，均输出 `ACCEPTED`。
- **影响**：J07 若按入库生成 DTO 构造/校验流事件，能发送违反 Q2/Q5 的事件；J08 按生成类型实现时不能依赖错误原因或 `retrieved=0`，可能走错误的撤回/提示分支。`gen-contracts.sh --check` 通过只证明生成物同步，不证明条件约束落入 DTO。交接已披露生成器忽略 `if/then`，但缺口仍是本次契约交付的消费者风险。
- **最小修复**：将条件约束改为生成器可表达的判别分支，或在 J07/J08 消费生成 DTO 的边界显式校验；给生成 Python 模型与 TS 类型增加反例测试，不仅测 `jsonschema` 真源。

### B13-R02 — P2：`meta(answered)` 可宣称零证据进入生成

- **目标提交/位置**：`14d405d`；`src/contracts/api.v1.yaml:2164-2182`，对应生成物 `src/contracts/v1/generated/schemas/ChatEvent.schema.json` 的 `ChatMetaEvent`。
- **触发**：`{"event":"meta","status":"answered","retrieved":0,"graph_version":3,"request_id":"r1"}` 经入库独立 JSON Schema 校验返回 `True`；新增测试仅覆盖 `not_covered` 时 `retrieved=0`。
- **影响**：Q2 把 `retrieved` 定义为允许引用集合 `A` 的大小，`answered` 代表进入生成；空 `A` 没有可引用证据，却被契约视为合法生成分支，掩盖 P5 应拒答而不调用模型的错误。
- **最小修复**：给 `status=answered` 分支加 `retrieved >= 1`，并用零值反例测试真源及生成产物；仍保留 `not_covered` 分支的零值约束。

## 验证与边界

- 从目标固定提交 `git archive 14d405d` 到 `/private/tmp/smartsketch-b13-review.enHypy`，未修改 Claude worktree、未安装依赖、未访问数据库或模型。
- `/opt/anaconda3/bin/python -m pytest tests/contracts/test_b13.py -q`：**43 passed**。
- `PATH=/opt/anaconda3/bin:$PATH ./scripts/verify.sh`：exit 0；门禁负例 22 项，B08 5、B09 5、B10 36、B13 43 项通过，生成物一致。`git diff 3771ae1 14d405d --check`：exit 0。
- 反例直接运行入库 `ChatEvent.schema.json` 和生成的 Pydantic 模型；上述问题未被现有绿色门禁覆盖。没有运行尚未实现的问答服务端、前端或真实存储测试。

## 后续

修复 B13-R01/R02 后按新固定提交复审；B10-R01/R02、B13 之后的父分支集成、A08/A10/meta 等既有待审范围仍独立排队。本报告不是整轮或整分支通过声明。
