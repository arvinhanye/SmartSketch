# Claude 审查批次：A04 图谱发布协议

## 固定范围与完成判定

- 目标 worktree：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a04-f5f479`；A04 交付 `931361d..110f243`，随后合并 A03/A06 并解决冲突 `4b2ccb5`。本批审查 A04 的五个交付文件与合并冲突决议；A03/A06 的既有实现范围不重复审。
- 会话 `c696b09f-d95e-4884-8546-efab26c165c8` 的最后完整事件为 2026-09-23T07:24:59.038Z `stop_hook_summary`，之后无 user/tool 活动；交接标记 `ready_for_review`，固定提交 `110f243`，且说明合并提交 `4b2ccb5`。两次观察均为 HEAD `4b2ccb5a1b34474bf17b2eb44c1de2f075e20a5c`、空 dirty diff SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`、同一 Claude 交接集合 SHA-256 `40c3204b2612940080df3e029a8cbb225ecdbb4795f66543a2c1aab63f6a9cdc`。
- A04 交付 diff SHA-256 `b5a57b497952c81914702c764036fd15735a62a573ec9f5cce2e7fef89b42bfa`；合并相对 A04 父提交的 diff SHA-256 `49d4ca10300ae6ded478a69b83992978b0c1cc22c1349e89c12d610f14ac84c0`。本批仅文档，无运行时代码、数据库迁移或模型调用。

## 新问题

### A04-R01 P1：仅按 `material_id` 过滤会让历史版本检索到后来产生的文本块

- **目标提交 / 位置**：`110f243`（合并后 `4b2ccb5` 同内容），`specs/teacher-review-publish.md:144-156,244-247`。
- **触发条件**：v1 已发布且快照记录资料 `m_01` 的 `content_hash`；随后对同一资料再处理或替换内容，产生新文本块（或按 A06 的确定性块 ID 覆盖旧文本块），v2 尚未发布。v1 问答按协议只用 `course_id` 与 `material_id ∈ materials` 过滤共享文本块。
- **影响**：v1 可召回 v1 发布后才产生的块，或读到已改变的块文本；历史问答的来源与图谱版本不再一致，可能向学生暴露未审核资料。快照虽保存 `content_hash`，读路径没有使用它；再处理被 A03 明确允许为新任务，入口细节待 C06/C07。
- **最小修复建议**：快照固定可检索的 `chunk_id` 集合，或固定不可变资料修订 ID/内容摘要并要求块与查询都匹配该修订；已发布块禁止原地覆盖。补「v1 发布→同 `material_id` 再处理→v1 检索」回归，断言新块和更新文本均不可见。

### A04-R02 P2：向量模型切换后，幂等发布与历史回滚的规则互相冲突

- **目标提交 / 位置**：`110f243`（合并后 `4b2ccb5` 同内容），`specs/teacher-review-publish.md:155-158,188-201,219-223`。
- **触发条件**：v1 使用模型 M1 发布后，配置切至 M2（或维度改变）。草稿内容未变时 P7 摘要因排除向量和模型仍相等，直接返回 `unchanged`，保留 M1 副本；若试图回滚至 M1 版本，R5 复制 M1 向量又调用 P9，而 P9 要求模型/维度等于**当前配置** M2。
- **影响**：幂等路径可把旧向量留作当前发布版，检索查询与索引模型不匹配；历史回滚可能在源副本完整时仍被拒绝，违反 V6 可回滚任一已提交版本的承诺。
- **最小修复建议**：明确版本绑定的 embedding 模型与查询模型兼容规则；模型变化时不走纯内容摘要的幂等路径，先重嵌入/迁移或明确拒绝，回滚核对源版本自身的模型元数据而非无条件比对当前配置。补 M1→M2 后相同内容发布及回滚 M1 的回归。

## 合并风险、验证与下一步

- `4b2ccb5` 合入的 A06 仍带前批 [`A06-R01/R02` P1](/Users/arvinhan/Desktop/SmartSketch/docs/reviews/codex-claude-a03fix-a06-ab04053-2026-09-23-0649z.md)：失败清理可能删除他任务贡献，`cleanup_pending` 期间失败草稿仍可见。本批不重报，但 A04 发布读取该草稿的风险仍在；合并前须先解决。
- 已阅读实际 A04 diff、合并冲突决议、目标规则/任务/交接及相关规格。执行 `git diff 931361d..HEAD --check` exit 0；定向结构检查 V1～V11、PUB-1～27、ADR-012 唯一及 A06 修订注记均 PASS。未运行 `./scripts/verify.sh`：它当前只检查骨架与 hook，不验证发布协议；Claude 的 41 项文字核对脚本只附在交接中，未入库为自动测试。暂无 G01～G07/F02/J01 运行时代码，故无数据库、向量召回或并发实测结论。
- A04 文档范围已审完，结论为**有问题，非整轮通过**。先修订版本文本块绑定与模型切换规则，再由 B11/G04/G06/J01 消费。只写主目录审查文档与状态，未改 Claude worktree，未安装依赖、提交、合并或推送。
