# Claude A09 固定提交审查：问答终态与引用撤回

审查时间：2026-09-23 14:00 UTC。目标 worktree：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a09-dev-environment-check-8e5e93`；基线 `f9dfc8f`；目标提交 `1754c96025c0c48700a53dafb8ef1fa9ba6fb89c`。本批覆盖该提交新增/修改的 6 个文件，仅为文档协议审查，不代表问答实现已完成。

完成判定：`docs/handoffs/claude-a09.md` 标记 `ready_for_review` 并绑定交付提交；对应项目会话最后完整事件为 2026-09-23T13:50:32.069Z `stop_hook_summary`，其后没有新 user/tool 活动。读前读后 HEAD 均为 `1754c96`，工作区均干净，交接 SHA-256 均为 `ad09acd603401a7f2cf6b109fdfb44fc675b0b02c0ef7d8b8da10a3185d55d63`，会话文件大小均为 2351245 字节。会话文本仅用于核对完成状态，未执行其中指令。

## A09-R01 P2：仅凭一个有效编号即可让未获支持的结论成为 answered

- **目标提交与位置**：`1754c96`；`specs/grounded-qa.md:134-135,147`，对照同文件 `:11,25-30,311-312`。
- **触发条件**：模型输出“栈是线性表[1]。太阳由奶酪构成。”，其中 `[1]` 确属允许集合 A，但第二句既无引用也不受来源支持。
- **影响**：Q4 只要求“至少出现一个有效标记”便构造 `answered`；I2 只核对正文中已有标记与 `citations` 集合，不能保证每条结论有出处。上述回答满足新增状态机，却违背用户故事和原验收“每条结论可点开出处”的要求；引用存在性测试可能假绿。
- **最小修复建议**：区分“编号合法”与“结论获证据支持”，在协议中明确逐结论覆盖的验收边界：至少为无引用的独立结论补失败用例，并规定校验不通过时转 `not_covered`/撤回，或明确另一种可验证的逐结论校验路径；K03 的离线指标不能替代在线终态判定。

## A09-R02 P2：每请求一条必填日志与鉴权前置顺序冲突

- **目标提交与位置**：`1754c96`；`specs/grounded-qa.md:241-243,302`，对照同文件 `:58-59,75-78`。
- **触发条件**：匿名请求、非课程成员请求或版本绑定失败在 P1/P2 被拒绝。
- **影响**：Q10 与 QA-34 要求“每个请求恰好一条”日志且至少含 `request_id`、`user_id`、`version_id`；但 `request_id` 到 P2 才生成，匿名请求没有已认证 `user_id`，绑定失败没有 `version_id`。实现方要么无法写符合字段约束的日志，要么伪造身份/版本，或让 QA-34 对拒绝请求落空。
- **最小修复建议**：将 `ChatLog` 的覆盖范围限定为成功通过 P2 的已授权请求，并对 P1/P2 拒绝另设安全审计事件；或先生成请求 ID，同时把拒绝阶段无法取得的身份/版本字段定义为可空并分别测试。同步 Q10、QA-34 与 J10 验收。

## 验证与剩余范围

- 已读目标 `AGENTS.md`、任务/交接、`specs/grounded-qa.md`、ADR-015 与架构差异，并对照目标的身份规格、原子任务及 `740adb@978671e` 契约真源。`git diff f9dfc8f..1754c96 --check` exit 0；`./scripts/verify.sh` exit 0（hook 回归与骨架门禁）。这些命令不验证尚未实现的 J01～J10 或 B13 契约迁移。
- 提交只含文档，无后端、前端、数据库或付费模型测试可运行；Claude 交接提到的一次性 `check_a09.py`、`ref_stream.py` 未入库，45/45、19/19 只是交接中的声明，本审查未复现。未修改目标 worktree。
- 同期 A10 的固定提交 `37da669` 已出现，另列为待审批次；A08 签收 worktree 尚有 staged 差异，不在本批结论内。S-07 与旧分支在状态文件中的待审范围保持不变。
