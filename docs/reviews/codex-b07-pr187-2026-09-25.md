# B07 / PR #187 审查复核（2026-09-25 UTC）

- 范围：`28b09b4..61197d9` 的七个文件；以 `origin/main@588d00a` 建临时合并副本复核，不触碰主目录未提交改动。
- 结论：未发现 P1/P2 问题。`src/backend/pyproject.toml` 自动合并后保留 C13 的 `argon2-cffi==25.1.0`，并加入 B07 锁定的三项测试依赖；无冲突标记或空白错误。
- 契约边界：普通执行缺 PyYAML、OpenAPI 校验器或 jsonschema 均打印 `FAIL` 且非零退出；显式 `--allow-scaffold` 缺依赖才打印 `SKIP` / `INCOMPLETE`；坏引用、坏关系枚举及畸形结构走失败路径；聚合入口打印最终 `PASS contracts gate` 或 `FAIL contracts gate`。这些行为由 B07 定向测试覆盖。

## 复核命令与结果

| 命令（临时合并副本） | 结果 |
| --- | --- |
| `python -m pytest tests/tooling/test_b07.py -q --tb=short` | 14 passed |
| `./scripts/verify.sh` | exit 0，含 24 项契约负例及 B08/B09/B10/B13 回归；输出 `PASS contracts gate` |
| `python -m pip check` | 无依赖破损 |
| `python -m pytest tests -q --tb=short` | 620 passed，1 条 Starlette 上游弃用警告 |
| `git diff --cached --check` | PASS |

首次全量测试在受限沙箱中为 618 passed、2 failed；两例 C13 测试在调用 `socket.bind(("127.0.0.1", 0))` 时被沙箱拒绝，尚未触及被测服务。允许本地 socket 的复跑 620 passed。CI 状态在合并前还须以 PR 最新 HEAD 重新确认。
