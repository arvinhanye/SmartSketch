# Claude B10 固定提交审查（2026-09-24 07:03 UTC）

## 范围与结论

- 目标：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/a09-dev-environment-check-8e5e93` 的固定提交 `3771ae1f576b88bfe6f4798a17b00fa32e3179b1`，基线 `9d2437ec6f2ad6af736df669741e16d887913054`。交接：`docs/handoffs/claude-b10.md`（该提交中的 SHA-256 `67e43c8f03e6564ff26fdd09332df5e7955f2277eab60e901b33392ae472c33f`）。
- 仅审 B10 的 13 个变更文件；后续 B13、父 worktree 当前 HEAD `14d405d`、未跟踪的 `egg-info`、其他旧待审范围均不在此结论内。固定提交从归档副本验证，未修改 Claude worktree。
- 发现 **2 项 P2**：阶段结束进度与取消终态标志缺少结构约束。无整分支通过结论。

## 问题

### B10-R01 — P2：`awaiting_review` 结束事件接受错误进度

- **目标提交/位置**：`3771ae1`，`src/contracts/api.v1.yaml:1358-1363`（`TaskStageEvent.stage/progress`）。
- **触发条件**：SSE 发送 `{"task_id":"t","stage":"awaiting_review","progress":0.1,"cancel_requested":false}`。独立 JSON Schema 验证结果为合法；但 `specs/task-processing.md` §1/§7 固定该阶段 `progress = 0.95`，且此事件发完立即关流。
- **影响**：前端把合法契约载荷当最终处理进度并关闭连接，可永久停留在 10%；B10 的正例只测 0.95，没有反例。生成的 Python/TypeScript 类型同样只限制为普通数字。
- **最小修复**：将 `awaiting_review` 从普通阶段事件拆为 `progress: const 0.95` 的分支，或用条件 schema 约束该阶段；重新生成产物并增加 `awaiting_review + 0.1` 必须拒绝的回归。

### B10-R02 — P2：取消终态快照接受 `cancel_requested = false`

- **目标提交/位置**：`3771ae1`，`src/contracts/api.v1.yaml:1290-1305,1319-1321`（`Task`）。
- **触发条件**：含必填 ID/时间戳的 `Task` 取 `stage: cancelled`、`cancel_requested: false`。独立 JSON Schema 验证结果为合法；同一提交的 `TaskCancelledEvent` 已用 `const: true` 拒绝相同矛盾，而 `specs/task-processing.md` §4 的所有取消路径均先置标志。
- **影响**：GET/取消端点可返回与 SSE 不一致的合法快照，客户端重连或轮询后无法以标志准确表示取消结果；现有测试只验证标志必填，不验证终态值。
- **最小修复**：在 `Task` 上以 `allOf` 追加 `stage = cancelled ⇒ cancel_requested = true`，保留 `failed ⇔ error` 现有条件；增加负例及正例，重新生成产物。

## 验证与限制

- 在 `/private/tmp/smartsketch-b10-review.9kypmz` 由 `git archive 3771ae1` 建立隔离副本。`/opt/anaconda3/bin/python3 -m pytest -p no:cacheprovider tests/contracts/test_b10.py -q`：**36 passed**。
- `PATH=/opt/anaconda3/bin:/usr/local/bin:/usr/bin:/bin PYTHONDONTWRITEBYTECODE=1 ./scripts/gen-contracts.sh --check`：**生成物与真源一致**；`git diff 9d2437e 3771ae1 --check`：通过。
- 默认 `python3` 缺 `pytest`，第一次调用在收集前失败；改用已有 Anaconda 环境，未安装依赖。两个定向负例在该环境用 `jsonschema.Draft202012Validator` 重现为 `valid=True`。
- 本批验证结构契约、生成物一致性与 B10 测试；票据核销、任务状态机、权限隔离及 SSE 运行时尚未由 B10 实现，未以契约测试代替运行时验收。生成的 Pydantic `Task` 忽略 `if/then/else`，交接已列为 C10/C11 的运行时校验要求。
