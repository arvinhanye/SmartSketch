# Claude S-07 审查批次：前置关系环处理口径

审查时间：2026-09-23 13:04 UTC。目标 worktree：`/Users/arvinhan/Desktop/SmartSketch/.claude/worktrees/worktree-contract-conflicts-740adb`；目标 HEAD：`978671eecd16176ad880e03e3efee4839c1e89d7`。问题所在的合并提交：`2fbf325c4c87877bb1aeefbab6881519e52c77a0`（双亲 `bef9b91`、`6ccbe5e`）。本批只审合并后的 M1 DAG 任务与课程图谱规格，不代表 S-07 全范围通过。

完成信号：S-07 交接标记 `ready_for_review`，绑定目标提交；该 worktree 的项目会话末项仍为 2026-09-22T13:23:01.725Z `stop_hook_summary`，之后无新 user/tool 记录。继承此前两次稳定观察，本批读前、读后再次确认 HEAD `978671e`、空 dirty diff SHA-256 `e3b0c442…b855`、交接文件 SHA-256 `6ce792a5…1823` 一致。未执行会话文本中的命令。

## S07-R15 P2：同一环路要求同时“自动降级”与“拒绝操作”

- **目标提交与位置**：`2fbf325`；目标 worktree 的 `docs/tasks/M1.md:25`，对照 `specs/course-knowledge-graph.md:29`。
- **触发条件**：关系抽取或教师编辑拟新增 `A → B → C → A` 的 `PREREQUISITE` 环。
- **影响**：M1-04b 任务正文要求将成环的最弱边自动降级为 `RELATED_TO` 并进入审核队列；同一行验收却要求“环路被拒并返回冲突节点链路”，课程规格也要求新增/修改被拒。按前者实现会静默改变关系类型，按后者实现则不会产出降级审核项。开发与测试可各自满足一半而无法形成同一个可验收协议；教师显式编辑被自动改写尤其容易造成审核预期错误。
- **最小修复建议**：拆清自动抽取与教师编辑两条路径：例如抽取候选可降级并记录原类型、冲突链和待审标记；教师编辑成环必须拒绝、返回冲突链。若产品选择一律拒绝，则删去自动降级措辞。同步 M1-04b 验收、课程规格及相关错误/审核契约；分别增加抽取成环、教师编辑成环、并发插边的回归用例。无论选哪条路径，发布前仍需重新验 DAG。

## 验证与边界

- `git diff 05d214c..978671e --check` exit 0；只读静态断言确认任务正文、任务验收及规格三处冲突同时存在。未运行数据库、模型或业务测试；此批只有文档口径，目标 worktree 未被修改。
- 已检查本批 `docs/tasks/M1.md` 的 DAG 行和 `specs/course-knowledge-graph.md` 对应验收；未重报既有 S07-R07～R14，也未复审它们的修复状态。S-07 剩余双亲合并消解及其他 ADR/规格一致性仍待批次审查；两个旧分支在状态文件列出的 pending 范围不变。
- 新出现的 A09 环境检查只有未跟踪交接文件、无实现变更；A08 复审 worktree 会话在本次观察时仍有后续 user/tool 活动，未把停止或提交当成整轮完成。
