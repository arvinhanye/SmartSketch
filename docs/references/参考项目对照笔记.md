# 参考项目对照笔记（团队自撰）

> **这是团队自撰的实现研究，不含任何第三方源码**，只有文件路径与行号引用。
> 原件位于仓库外，本文件是 S-06 纳入版本控制的副本。

| | |
| --- | --- |
| 原件 | `/Users/arvinhan/Desktop/A10--数字马力杯--项目规划/参考项目/对照笔记.md` |
| 复制日期 | 2026-09-22 |
| 原件内容摘要 | `sha256:4e97ef2e6ea0`（前 12 位，用于判断原件是否已变更） |

## 阅读前提

1. **路径前提**：正文里 `llm-graph-builder/...` 与 `LightRAG/...` 开头的路径，相对于原件所在的
   `参考项目/` 目录，**本仓库不包含这些源码**。原文的 Markdown 链接已改写为纯路径引用，
   因为它们在本仓库中无法解析。
2. **只读，不复制源码**。实际借用代码时必须按根目录 `NOTICE` 的四项要求登记：
   具体文件、修改说明、保留版权与许可、补一条 ADR。
3. **项目名**：正文用的是早期名称「知径」，即现在的**智绘学途 / SmartSketch**。

## 已过时的结论（正文未改写，按此处为准）

| 正文位置 | 正文写法 | 现行决定 |
| --- | --- | --- |
| §8 图谱可视化 | 「知径用 G6 + 3d-force-graph」、标题含 2D / 3D | **仅 2D，Vue 3 + AntV G6**，见 [ADR-001](../decisions/ADR-001-vue3-g6-2d-graph.md) |
| §7 图数据库设计 | 关系名写作 `RELATED` | 本仓库为 **`RELATED_TO`**，四类关系见 AGENTS.md §1 |
| §10 学习路径推荐 | 「AIGC 讲解与练习题」列为待自研 | 已决议纳入，定位**加分项**，见 [ADR-007](../decisions/ADR-007-study-material-bonus-scope.md) |

其余结论（分块策略、抽取提示词、融合消歧、检索 Cypher、SSE 进度、APOC 依赖）与当前方案一致。
**§10 指出两个参考项目都没有学习路径推荐、需要自研**，这是 `specs/learning-path.md` 存在的直接理由。

---

# 知径 × 参考项目 代码对照笔记

- 对照对象
  - **LGB** = neo4j-labs/llm-graph-builder，本地位于 `参考项目/llm-graph-builder`，提交 `5ff7af3`（2026-07-30），Apache-2.0
  - **LR** = HKUDS/LightRAG，本地位于 `参考项目/LightRAG`，提交 `044eac0`（2026-09-16），MIT
- 链接文字里的 `:数字` 是行号，打开文件后跳到对应行。
- 知径模块编号沿用《知径项目详细方案》的章节号（§4–§14）。
- 结论标记：**借鉴** = 思路或代码可直接参考；**改造** = 要按知径需求改；**不采用** = 看懂即可，知径不这么做。

---

## 总览：一句话结论

| 知径模块 | 首选参考 | 结论 |
|---|---|---|
| §4 文档解析与分块 | LR 解析器 + 段落语义分块 | 改造：LGB 只按固定 token 切，LR 按标题层级切，更适合教材 |
| §5 知识抽取 | LR 提示词 + LGB 的“允许类型”约束 | 改造：两者都是一次同时抽实体和关系，知径拆成两阶段并加“前置”判定 |
| §6 融合消歧 | LGB 去重查询 + LR 描述合并 | 借鉴并补全：LGB 有相似度检测但需人工确认，LR 只按同名合并 |
| §7 图数据库 | LGB 的 Document/Chunk/实体三层模型 | 借鉴：几乎可以照搬，加 `course_id` 与教育专用关系 |
| §8 图谱可视化 | LR 前端（搜索、聚焦、布局切换、编辑弹窗） | 借鉴交互，不借鉴渲染库（知径用 G6 + 3d-force-graph） |
| §9 GraphRAG 问答 | LGB 检索 Cypher + LR 引用格式提示词 | 借鉴：两者组合正好是知径方案 |
| §10 学习路径推荐 | 无 | 两个项目都没有，需自研 |
| §11 教师修正 | LR 图谱编辑接口 + LGB 孤立节点清理 | 借鉴：LR 的增删改合并接口最完整 |
| §12 接口与进度推送 | LGB 的 SSE 进度推送 | 借鉴 |
| §13 提示词工程 | LR `prompt.py` | 借鉴结构，改写为中文教育场景 |
| §14 测试评估 | 两者都用 RAGAS | 借鉴问答评估；抽取准确率需自建标注集 |
| §15 部署 | LGB `docker-compose.yml` | 借鉴 |

---

## §4 文档解析与预处理

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/src/document_sources/local_file.py:36` `load_document_content` | PDF 用 PyMuPDFLoader；TXT 先检测编码；其他格式交给 Unstructured | **借鉴**编码检测（中文 TXT 常见 GBK）。PDF 只拿到纯文本，不保留标题层级 |
| LGB `llm-graph-builder/backend/src/create_chunks.py:29` `split_file_into_chunks` | `TokenTextSplitter` 按固定 token 数切块，逐页处理，记录 `page_number` | **不采用**切法：固定长度会把一个定义切成两半。可借鉴“按页切 + 记页码”的元数据 |
| LGB `llm-graph-builder/backend/src/make_relationships.py:60` `create_relation_between_chunks` | 块内容 SHA1 作为块 ID；建 `PART_OF`、`FIRST_CHUNK`、`NEXT_CHUNK` 关系 | **借鉴**：内容哈希做 ID 天然去重，也正好用于方案 §15 的“按块哈希缓存抽取结果” |
| LR `LightRAG/lightrag/parser/docx` | DOCX 解析：标题识别（`smart_heading`）、编号、表格、公式（`omml`） | **借鉴**：教材 DOCX 的公式、编号标题正好用得上 |
| LR `LightRAG/lightrag/parser/markdown` | Markdown 解析为带标题层级的中间结构 | **借鉴** |
| LR `LightRAG/lightrag/chunker/paragraph_semantic.py` + `LightRAG/docs/ParagraphSemanticChunking-zh.md` | 先按标题切成块，再按大小合并或拆分；大表格按行拆并补表头 | **重点借鉴**：就是方案 §4.1 说的“在章节边界内分块”，有现成中文算法说明 |
| LR `LightRAG/lightrag/prompt.py:51` `entity_extraction_section_context` | 把“章 → 节 → 小节”路径作为背景传给抽取提示词 | **借鉴**：对应知径文本块的 `section_path` 字段 |

**知径做法**：解析阶段参考 LR 保留标题层级，分块参考 LR 的段落语义分块，块 ID 和页码参考 LGB。

---

## §5 基于大模型的知识抽取

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/src/llm.py:249` `get_graph_from_llm` | 解析“允许的节点类型 / 关系三元组”，校验关系两端必须是允许类型 | **借鉴**：知径的 5 种实体类型、4 种关系就是这种约束 |
| LGB `llm-graph-builder/backend/src/llm.py:195` `get_graph_document_list` | 调用 LangChain `LLMGraphTransformer`，支持结构化输出时附带 `description` | **不采用**：封装太深，提示词不可控，不利于提交“提示词工程记录” |
| LGB `llm-graph-builder/backend/src/llm.py:158` `get_combined_chunks` | 把相邻几个块拼成一次调用 | 参考：可减少调用次数，但块变大会降低抽取质量，需实测权衡 |
| LGB `llm-graph-builder/backend/src/shared/schema_extraction.py:61` `schema_extraction_from_text` | 让 LLM 从样例文本中自动归纳实体类型和关系类型 | 可选：新课程初始化时给教师推荐类型，知径默认用固定类型 |
| LR `LightRAG/lightrag/prompt.py:175` `entity_extraction_json_system_prompt` | JSON 输出；实体含 name/type/description；关系只能引用已抽出的实体；限制输出条数；防止抄模板示例 | **重点借鉴**：结构清晰，改写成中文并加入知径的类型定义与“前置关系判定准则” |
| LR `LightRAG/lightrag/prompt.py:255` `entity_continue_extraction_json_user_prompt` | “补漏”提示：只输出上次遗漏或写错的内容 | **借鉴**：召回率不足时对重要章节追加一轮 |
| LR `LightRAG/lightrag/operate.py:3942` `extract_entities` | 按块并发抽取（`asyncio.Semaphore`，`LightRAG/lightrag/operate.py:4548`）；可选补漏轮（`LightRAG/lightrag/operate.py:4253`）；支持取消 | **借鉴**并发与取消逻辑，对应方案 §15 的 60 秒预算 |
| LR `LightRAG/lightrag/operate.py:662` `_normalize_and_validate_entity_type` | 规范化并校验模型返回的实体类型 | **借鉴**：对应 §5.5 规则后校验 |
| LR `LightRAG/lightrag/utils_graph.py:243` `_reject_self_loop_relation` | 拒绝自环关系 | **借鉴** |

**两者都没有的，知径要自研**
1. 两阶段抽取（先定实体表，再在表内抽关系）。
2. “前置关系”的判定准则与**环检测**：LR 默认把关系当无向处理（见 `LightRAG/lightrag/prompt.py:175` 第 3 条），LGB 也不检查环。
3. 每条关系的 `confidence` 与原文 `evidence`，用于教师审核和准确率核验。

---

## §6 知识融合与消歧

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/src/graphDB_dataAccess.py:470` `get_duplicate_nodes_list` | 一条 Cypher 找疑似重复：名称互相包含、编辑距离 < 3、或向量余弦 > 0.97；要求类型相同 | **重点借鉴**：就是知径“规则 + 向量”两级。中文名较短，编辑距离阈值和 0.97 需重新调 |
| LGB `llm-graph-builder/backend/src/graphDB_dataAccess.py:520` `merge_duplicate_nodes` | `apoc.refactor.mergeNodes` 合并节点并迁移关系 | **借鉴**：注意社区版 Neo4j 需安装 APOC 插件 |
| LGB 前端 `llm-graph-builder/frontend/src/components/Popups/GraphEnhancementDialog/Deduplication/index.tsx` | 列出疑似重复组，人工勾选后合并 | **借鉴**：对应知径“0.78–0.90 进入审核”的界面 |
| LGB `llm-graph-builder/backend/src/post_processing.py:149` `graph_schema_consolidation` + `llm-graph-builder/backend/src/shared/constants.py:827` `GRAPH_CLEANUP_PROMPT` | 让 LLM 把相近的节点类型、关系类型名称归并 | 参考：知径类型固定，用处不大 |
| LR `LightRAG/lightrag/operate.py:2429` `_merge_nodes_then_upsert` | **只按实体名完全相同合并**；合并类型、来源块、描述 | 参考：没有同义词识别，这是知径要补的 |
| LR `LightRAG/lightrag/operate.py:372` `_handle_entity_relation_summary` + `LightRAG/lightrag/prompt.py:297` | 同一实体多段描述过长时，用 LLM 总结成一段 | **借鉴**：知识点在多章出现时生成统一定义 |

**知径做法**：LGB 的检测查询（第 1、2 级）+ 新增 LLM 裁决（第 3 级）+ LR 的描述合并总结。

---

## §7 图数据库设计

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/src/make_relationships.py:35` `create_chunk_embeddings` | Chunk 节点存 `embedding`，`PART_OF` 指向 Document | **照搬** |
| LGB `llm-graph-builder/backend/src/make_relationships.py:12` `merge_relationship_between_chunk_and_entites` | `(Chunk)-[:HAS_ENTITY]->(实体)`，UNWIND 批量写入 | **照搬**：即知径的 `MENTIONED_IN`（方向相反，按需统一） |
| LGB `llm-graph-builder/backend/src/make_relationships.py:149` `create_chunk_vector_index` | 用 Neo4j 自带向量索引，不需要额外向量库 | **照搬**，与知径选型一致 |
| LGB `llm-graph-builder/backend/src/post_processing.py:85` `create_vector_fulltext_indexes` | 建全文索引和实体向量索引 | **借鉴**：问答时用全文索引做术语到知识点的链接 |
| LGB `llm-graph-builder/backend/src/shared/common_fn.py:281` `save_graphDocuments_in_neo4j` | 写入失败自动重试 | 借鉴 |
| LR `LightRAG/lightrag/kg/neo4j_impl.py:79` `Neo4JStorage` | 图存储抽象的 Neo4j 实现；`LightRAG/lightrag/kg/neo4j_impl.py:1301` 按标签取子图 | 参考：多存储抽象对知径过度设计 |

**知径要加的**：所有节点带 `course_id`；`Course`、`Chapter` 节点；`PREREQUISITE / CONTAINS / RELATED / EXAMPLE_OF` 四种教育关系；节点 `locked`、`confidence`、`source` 字段。LGB 的实体节点直接用 LLM 返回的类型作标签，知径统一为 `KnowledgePoint` 标签 + `type` 属性，查询更简单。

---

## §8 图谱可视化（2D / 3D）

两个项目都不是 3D，**只借鉴交互设计与数据接口**，渲染层按方案用 G6 与 3d-force-graph 实现。

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/src/graph_query.py:197` `get_graph_results` | 按文档取节点和关系，转成前端格式 | **借鉴**：对应知径 `/graph` 接口，额外返回 `level`、`chapter` |
| LGB `llm-graph-builder/backend/src/neighbours.py:40` `get_neighbour_nodes` | 取某节点邻居 | **借鉴**：详情抽屉中的前置、后续知识点 |
| LGB `llm-graph-builder/frontend/src/components/Graph/GraphViewModal.tsx:282` `mouseEventCallbacks` | 点击节点、缩放、平移、拖拽回调；`llm-graph-builder/frontend/src/components/Graph/GraphViewModal.tsx:59` 防抖搜索 | 参考交互清单 |
| LGB `llm-graph-builder/frontend/src/components/Graph/GraphPropertiesPanel.tsx` | 节点属性侧边面板 | 参考：知识点详情抽屉 |
| LR `LightRAG/lightrag_webui/src/features/GraphViewer.tsx:145` | sigma.js 图谱主视图，`LightRAG/lightrag_webui/src/features/GraphViewer.tsx:99` 悬停、点击事件 | 参考页面结构 |
| LR `LightRAG/lightrag_webui/src/components/graph/GraphSearch.tsx:228` | 节点搜索下拉框 | **借鉴**：知径顶部搜索 |
| LR `LightRAG/lightrag_webui/src/components/graph/FocusOnNode.tsx:8` | 搜索命中后镜头移到节点 | **借鉴**：3D 中改为 `cameraPosition` 飞行 |
| LR `LightRAG/lightrag_webui/src/components/graph/LayoutsControl.tsx` | 多种布局在 Web Worker 中计算、可切换 | **借鉴**：大图布局放 Worker，避免界面卡顿 |
| LR `LightRAG/lightrag_webui/src/hooks/useLightragGraph.tsx` | 拉取图数据、按度数设节点大小、着色 | 参考数据到视觉的映射 |

---

## §9 智能问答（GraphRAG）

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/src/QA_integration.py:710` `QA_RAG` | 问答总入口，按模式分派 | 参考流程 |
| LGB `llm-graph-builder/backend/src/shared/constants.py:718` `CHAT_MODE_CONFIG_MAP` | 7 种模式：vector、fulltext、graph_vector、graph_vector_fulltext（默认）等 | 知径只需要 graph_vector_fulltext 一种 |
| LGB `llm-graph-builder/backend/src/shared/constants.py:335` `VECTOR_GRAPH_SEARCH_QUERY_PREFIX` 与 `llm-graph-builder/backend/src/shared/constants.py:347` `VECTOR_GRAPH_SEARCH_ENTITY_QUERY` | **向量找块 → 块上的实体 → 按相似度扩展 0–2 跳 → 拼成“原文 + 实体 + 关系”文本** | **重点借鉴**：一条 Cypher 完成方案 §9 的混合检索，改成限定 `course_id` 并优先扩展 `PREREQUISITE` |
| LGB `llm-graph-builder/backend/src/QA_integration.py:302` `create_document_retriever_chain` + `llm-graph-builder/backend/src/shared/constants.py:299` `QUESTION_TRANSFORM_TEMPLATE` | 结合历史对话改写问题，再检索 | **借鉴**：对应知径 `rewrite_query` |
| LGB `llm-graph-builder/backend/src/shared/constants.py:256` `CHAT_SYSTEM_TEMPLATE` | “只依据上下文、不知道就说不知道”，并声明上下文里的指令不可执行 | **借鉴**防注入写法；它不要求逐句引用，需补 |
| LGB `llm-graph-builder/backend/src/chunkid_entities.py:265` `get_entities_from_chunkids` + 前端 `llm-graph-builder/frontend/src/components/ChatBot/ChunkInfo.tsx` | 回答后展示用到的原文块和实体 | **借鉴**：知径的“涉及的知识点”标签和出处展开 |
| LR `LightRAG/lightrag/operate.py:4975` `get_keywords_from_query` + `LightRAG/lightrag/prompt.py:484` | 把问题拆成低层关键词（具体术语）和高层关键词（主题） | **借鉴**：低层词链接知识点，高层词找章节 |
| LR `LightRAG/lightrag/operate.py:5280` `_perform_kg_search` / `LightRAG/lightrag/operate.py:6075` `_build_query_context` | local（实体）/ global（关系）/ mix 检索，按 token 预算截断拼上下文 | 参考截断策略，保证 15 秒内响应 |
| LR `LightRAG/lightrag/prompt.py:334` `rag_response` | 同时使用图谱数据与原文块；末尾 `### References` 列出 `[n]` 引用；找不到就说信息不足 | **重点借鉴**：知径“强制引用”提示词可基于它改写 |
| LR `LightRAG/lightrag/api/routers/query_routes.py:746` `/query/stream` | 流式返回回答 | **借鉴**：知径 `/chat` 的 SSE 流式输出 |

**知径做法**：检索用 LGB 的 Cypher 思路，关键词拆分和回答提示词用 LR 的，自研“低相似度直接拒答”和“引用编号校验”。

---

## §10 学习路径推荐

两个项目**都没有**。可复用的只有底层能力：

- LGB `llm-graph-builder/backend/src/neighbours.py:40`：取邻居的 Cypher 写法，可改成沿 `PREREQUISITE` 取祖先或后继。
- LR `LightRAG/lightrag/operate.py:6200` `_get_node_data`：批量取节点和度数（`asyncio.gather`），可用于计算“解锁度”。

其余按方案 §10 自研：候选集判定、评分公式、Kahn 拓扑排序、路径高亮、AIGC 讲解与练习题。

---

## §11 教师端图谱修正

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LR `LightRAG/lightrag/utils_graph.py:1550` `aedit_entity` | 修改实体（含改名），同步更新向量库和来源追踪 | **重点借鉴**：改名后关系、向量要一起更新，容易漏 |
| LR `LightRAG/lightrag/utils_graph.py:1850` `aedit_relation` / `LightRAG/lightrag/utils_graph.py:2293` `acreate_entity` / `LightRAG/lightrag/utils_graph.py:2487` `acreate_relation` | 编辑关系、新建实体、新建关系 | **借鉴**：新建前置关系时在此处加环检测 |
| LR `LightRAG/lightrag/utils_graph.py:3440` `amerge_entities` | 人工合并实体，合并属性与关系 | **借鉴** |
| LR `LightRAG/lightrag/utils_graph.py:539` `adelete_by_entity` | 删除实体及关联关系 | 借鉴 |
| LR `LightRAG/lightrag/api/routers/graph_routes.py:341` 起 | `/graph/entity/edit`、`/graph/relation/edit`、`/graph/entity/create`、`/graph/relation/create`、`/graph/entities/merge` | **借鉴接口设计** |
| LR 前端 `LightRAG/lightrag_webui/src/components/graph/PropertyEditDialog.tsx:31`、`LightRAG/lightrag_webui/src/components/graph/MergeDialog.tsx:27`、`LightRAG/lightrag_webui/src/components/graph/PropertiesView.tsx:14` | 属性编辑弹窗、合并弹窗、属性面板 | **借鉴**界面 |
| LGB `llm-graph-builder/backend/src/graphDB_dataAccess.py:430` `list_unconnected_nodes` + 前端 `llm-graph-builder/frontend/src/components/Popups/GraphEnhancementDialog/DeleteTabForOrphanNodes/index.tsx` | 列出并删除孤立节点 | **借鉴**：对应 §5.5 孤立节点检查 |

**知径要加的**：节点加锁（自动流程不覆盖）、发布前快照与回滚、草稿与发布状态。

---

## §12 接口设计与进度推送

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/score.py:614` `/upload` | 大文件分片上传后合并 | 可选：教材 PDF 较大时有用 |
| LGB `llm-graph-builder/backend/score.py:227` `/extract` → `llm-graph-builder/backend/src/main.py:441` `processing_source` | 分批处理块，每批更新 `processed_chunk`，每批前检查是否已取消 | **借鉴**：进度百分比与取消 |
| LGB `llm-graph-builder/backend/score.py:672` `/update_extract_status/{file_name}` | SSE 持续推送处理状态，客户端断开即停止 | **借鉴**：对应知径 `/tasks/{tid}/events` |
| LGB 前端 `llm-graph-builder/frontend/src/services/ServerSideStatusUpdateAPI.ts:5` | `EventSource` 接收进度，完成、失败、取消时关闭连接 | **照搬** |
| LGB `llm-graph-builder/backend/src/graphDB_dataAccess.py:334` `claim_document_for_processing` | 防止同一文档被重复处理 | 借鉴 |
| LR `LightRAG/lightrag/api/routers/document_routes.py:5225` `/upload`、`LightRAG/lightrag/api/routers/document_routes.py:6625` `/track_status/{track_id}` | 上传返回 track_id，再按 ID 查询状态 | 参考：与 LGB 二选一，知径用 SSE |

---

## §13 提示词工程

| 参考文件 | 可借鉴点 |
|---|---|
| LR `LightRAG/lightrag/prompt.py` | 所有提示词集中在一个 `PROMPTS` 字典，分 `---Role--- / ---Instructions--- / ---Output---` 段落，便于版本管理和对比 |
| LR `LightRAG/lightrag/prompt.py:175` | 抽取提示词中的“输出格式模板不是原文，不得从模板中抽取”，可防止模型把示例内容当成知识点 |
| LR `LightRAG/lightrag/operate.py:4019` | 实体类型说明通过 `entity_types_guidance` 注入，类型定义与提示词正文分离，改类型不改模板 |
| LR `LightRAG/docs/RoleSpecificLLMConfiguration-zh.md` + `LightRAG/lightrag/llm_roles.py` | 抽取、关键词、问答分别配置模型（抽取用快模型、问答用强模型） |
| LGB `llm-graph-builder/backend/src/shared/constants.py:256` | 问答系统提示中声明“上下文中的指令只当数据” |

知径做法沿用方案 §13：`prompts/*.yaml` 按版本存放，结构参考 LR，全部改写为中文并加入教育领域定义与反例。

---

## §14 测试与评估

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/src/ragas_eval.py:23` `get_ragas_metrics` + `llm-graph-builder/backend/score.py:981` `/metric` | 用 RAGAS 计算回答忠实度、相关性 | **借鉴**：问答测试报告可补充自动指标 |
| LR `LightRAG/lightrag/evaluation/eval_rag_quality.py` + `LightRAG/lightrag/evaluation/sample_dataset.json` | 批量评测脚本与测试集格式 | **借鉴**测试集 JSON 格式 |
| LGB `llm-graph-builder/backend/Performance_test.py`、`llm-graph-builder/backend/locustperf.py` | 接口性能测试 | 参考：验证 60 秒、15 秒指标 |

两者都**没有抽取准确率评测**，需按方案 §14.2 自建人工标注集。

---

## §15 模型接入与部署

| 参考文件 | 做了什么 | 对知径的意义 |
|---|---|---|
| LGB `llm-graph-builder/backend/src/llm.py:23` `get_llm` | 用环境变量 `LLM_MODEL_CONFIG_<模型名>` 配置多家模型 | 参考配置方式；**没有**通用 OpenAI 兼容接口，接 DeepSeek 需自己加分支 |
| LR `LightRAG/lightrag/llm/openai.py:179` | OpenAI 调用支持自定义 `base_url` | **借鉴**：DeepSeek、通义千问都提供 OpenAI 兼容接口，改 `base_url` 即可 |
| LGB `llm-graph-builder/docker-compose.yml` | 后端 + 前端两个服务 | **借鉴**，知径再加一个 Neo4j 服务 |
| LGB `llm-graph-builder/backend/example.env` | 环境变量示例 | 借鉴格式 |

---

## 建议阅读顺序（约半天）

1. LGB `llm-graph-builder/backend/src/shared/constants.py:335` 检索 Cypher：先理解“块—实体—关系”怎么一起查出来。
2. LGB `llm-graph-builder/backend/src/make_relationships.py` 全文（170 行）：图模型怎么写入。
3. LR `LightRAG/lightrag/prompt.py:175–300`、`LightRAG/lightrag/prompt.py:334`：抽取与回答提示词。
4. LGB `llm-graph-builder/backend/src/graphDB_dataAccess.py:470–540`：去重与合并。
5. LR `LightRAG/docs/ParagraphSemanticChunking-zh.md`：分块算法。
6. LR `LightRAG/lightrag/utils_graph.py:1550` `aedit_entity`：编辑时要同步哪些数据。
7. LGB `llm-graph-builder/backend/score.py:672` + `llm-graph-builder/frontend/src/services/ServerSideStatusUpdateAPI.ts`：进度推送前后端。

## 不建议投入时间的部分

- LGB：`communities.py`（社区检测与摘要，依赖 Neo4j GDS 企业插件）、`document_sources/` 下的 S3、GCS、YouTube、Wikipedia 来源、`auth_middleware.py`、Diffbot 相关。
- LR：`kg/` 下除 `neo4j_impl.py` 外的十几种存储实现、`parser/external/`（MinerU、Docling 外部服务）、多模态（VLM）、`tools/` 运维修复脚本、`k8s-deploy/`。

## 许可证

- LGB 为 Apache-2.0：复制或改写代码需保留原版权声明，并注明修改。
- LR 为 MIT：复制需保留版权声明。
- 建议在知径仓库的 `NOTICE` 或 README 中列出参考来源，并在提交材料中说明。
