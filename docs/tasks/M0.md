# M0 协作与应用骨架

> 状态与列的含义见 [`docs/tasks.md`](../tasks.md)。改自己认领的那一行，**不要重排或对齐整张表**。

## 里程碑窗口

对齐参考方案 S2 表 4.2 的 **M0 需求与设计：2026-09-16 ～ 09-19**，交付物含「开发环境与仓库搭建、项目骨架」。

> **编号对照（重要）**：仓库的 M0/M1 与 S2 的 M0/M1 含义不同，不能按编号直接对应。
> 仓库 M0「协作与应用骨架」只覆盖 S2 M0 交付物里的「项目骨架 / 开发环境」一项，
> S2 M0 的其余内容（需求确认、方案初稿、系统设计）不在本仓库的任务板上。
>
> **该窗口已于 2026-09-19 关闭，今日 2026-09-22 已逾期 3 天。**
> 未完成项同时是 S2 M1（09-20 ～ 09-26，主链路打通）的前置，实际必须在 09-26 前补齐。
>
> **`M0-04a` ～ `M0-04d` 的含义在 2026-09-22 合并两个 worktree 时统一为「四件契约产物」**
> （tech-plan 分支的编号）。另一分支原先用 `M0-04a`/`M0-04b` 指「起草契约真源」与「前端消费生成类型」，
> 这两件事改编为 **M0-09** 与 **M0-10**，避免同号异义。

## 任务

| ID | 状态 | 任务 | 负责人 | 截止日期 | 阻塞关系 | 验收条件 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M0-01 | DONE | 建立多 Agent 协作、文档、规格、源码目录骨架 | Codex | 2026-09-19 | — | 必需文件齐全；基础校验通过 | `scripts/verify.sh`；`docs/handoffs/codex-m0-project-scaffold.md` |
| M0-02 | TODO | 初始化 Vue 3 + TypeScript + Vite 前端 | Frontend Agent | 2026-09-19（已逾期） | 阻塞 M0-10、M1-07 | 可启动；具备最小路由、类型检查与测试命令 | 待补充 |
| M0-03 | TODO | 初始化 FastAPI 后端与健康检查 | Backend Agent | 2026-09-19（已逾期） | 阻塞 M1-01；**被 D-03（登录方案）阻塞** | 可启动；`GET /health` 有契约和测试 | 待补充 |
| M0-04a | DONE | REST DTO 定稿：课程/资料/任务/图谱/知识点/关系/进度/推荐/问答 | Claude | 2026-09-22 | 阻塞 M0-09 | `src/contracts/api.v1.yaml` 可被 OpenAPI 工具解析 | openapi-spec-validator 0.9.0：OpenAPI 3.1.0 valid，22 paths / 52 schemas / 181 refs；`docs/handoffs/claude-m0-04-api-contracts.md` |
| M0-04b | DONE | SSE 事件契约：事件名、顺序保证、心跳、终止与重连 | Claude | 2026-09-22 | 阻塞 M0-09 | 覆盖 ADR-005 全部状态枚举与终止条件 | `src/contracts/events.v1.md`；状态机 9 个取值全覆盖，三终态互斥且各恰一次 |
| M0-04c | DONE | 图谱交换 JSON：nodes（含 `level`/`type`/`confidence`）、edges、evidence、stats | Claude | 2026-09-22 | 阻塞 M0-09 | 前端 G6 适配层可直接消费 | `GraphExchange` schema，含 `format_version` 与图谱质量体检 `GraphStats` |
| M0-04d | DONE | 错误码表：闭集，每码有 HTTP 状态、可读消息与触发条件 | Claude | 2026-09-22 | 阻塞 M0-09 | 错误码闭集且不混入领域状态 | `src/contracts/errors.v1.md`；`NOT_COVERED`/`TASK_FAILED` 判定为领域状态而非错误码 |
| M0-05 | BLOCKED | 定义 Neo4j/SQLite 开发环境与本地启动方式 | Claude（数据/后端） | 2026-09-19（已逾期） | 阻塞 M1-05（APOC）；自身被「本机无容器运行时」阻塞 | 无密钥可启动依赖；环境变量文档完整；容器内 `RETURN apoc.version()` 返回版本号 | compose、脚本与文档已交付且 `./scripts/verify.sh` 通过；**APOC 实测未完成**，见 `docs/handoffs/claude-m0-05-local-env.md` |
| M0-06 | TODO | 按 ADR-004 ～ ADR-008 同步 `specs/course-knowledge-graph.md` 与其余规格 | 产品/协调 Agent | 2026-09-26 | 依赖 S-01、S-07（均已完成） | 各规格的状态序列与 ADR-005 一致，`cancelled` 按 ADR-006 记为正常可达终态并有取消端点；「待细化」中失效的 M0-04 引用改为具体子任务号 | 部分完成：`specs/course-knowledge-graph.md` 验收条件 2 已在 S-07 同步；其余规格待查 |
| M0-07 | DONE | 修正 `src/contracts/README.md` 的两处失效表述 | Claude（协调） | 2026-09-22 | — | ① 指向已删除 `docs/decisions.md` 的引用改为 `docs/decisions/ADR-...md`；② 状态机章节按 ADR-006 改写，删掉「预留、MVP 不产生、没有取消端点」 | 随 S-07 的契约冲突消解一并完成；`docs/handoffs/claude-s07-contract-conflicts.md` |
| M0-08 | DONE | 建立 `prompts/` 与 `evaluation/` 资产目录与规范 | Claude | 2026-09-22 | — | 目录、格式约定、提示词清单、评测指标与消融表模板就位 | `prompts/README.md`、`prompts/MANIFEST.md`、`evaluation/README.md`；已纳入 `scripts/verify/manifests/core.txt` |
| M0-09 | TODO | 实现 `scripts/gen-contracts.sh` 与入库生成物（openapi.json / JSON Schema / Pydantic / TS） | Backend Agent | 2026-09-26 | 依赖 M0-04a ～ M0-04d；阻塞 M0-10、M1-01 | 生成可重复（同一份 YAML 生成两次字节一致）；`--check` 在漂移时非 0 退出；缺生成器时**失败而不是跳过**，仅 `--allow-scaffold` 可降级并打印未完成标记（ADR-004 第 6 条） | 待补充 |
| M0-10 | TODO | 前端消费生成类型并回送契约反馈 | Frontend Agent | 2026-09-26 | 依赖 M0-09、M0-02 | `src/frontend/` 只从 `src/contracts/v1/generated/typescript/` 导入契约类型，无本地重定义或 `any` 绕过；任务状态穷尽分支含 `cancelled`；反馈写入交接文件 | 待补充 |
| S-01 | DONE | 确定 `src/contracts/` 的契约格式与单一真源，并拆分 M0-04 | Claude（协调） | 2026-09-22 | — | ADR-004、ADR-005 已记录；契约目录与命名规则可供冷启动 Agent 使用 | `./scripts/verify.sh`；`docs/handoffs/claude-s01-contract-format.md` |
| S-03 | DONE | 拆分单文件写争用点，`verify.sh` 改造为分发器 | Claude（协调） | 2026-09-22 | — | ADR/任务/未决问题无丢失迁移；`./scripts/verify.sh` 通过且纯排版编辑不再致其失败 | `./scripts/verify.sh`；`docs/handoffs/claude-s03-collab-restructure.md` |
| S-06 | DONE | 落地两项范围决策：实现任务取消、学习材料生成纳入加分项 | Claude（协调） | 2026-09-22 | 修订 ADR-005 第 2 条；派生 M1-06、M2-11 | ADR-006、ADR-007 已记录并在索引标注修订关系 | `./scripts/verify.sh`；`docs/handoffs/claude-s06-scope-decisions.md` |
| S-07 | DONE | 合并两个 worktree 并消解契约冲突（codex 审查 R01 ～ R05） | Claude（协调） | 2026-09-22 | 阻塞 M0-09 | 只剩一个可编辑机器真源；ADR 撞号已重编；契约门禁缺依赖时失败；来源引用与问答事件的结构约束落进 schema；状态机在规格与架构一致 | `./scripts/verify.sh`；`docs/handoffs/claude-s07-contract-conflicts.md` |

## 说明

> **M0-05 阻塞原因**：本机未安装任何容器运行时（Docker / OrbStack / Podman 均不存在），无法启动 Neo4j 实测 APOC。
> APOC 是 M2-01 融合消歧（`apoc.refactor.mergeNodes`）的硬前置，未实证前不得标 DONE。
> 解除方式：在装有容器运行时的机器上执行 `./scripts/dev-up.sh`，把 `RETURN apoc.version()` 的真实输出补进交接文件即可转 DONE。

> **M0-09 与 M0-10 顺序执行，不并行。** `src/contracts/` 只由 M0-09 写入，`src/frontend/` 只由 M0-10 写入；
> 前端不得直接改契约，需求通过交接文件回送后端 Agent 改 `api.v1.yaml` 后重新生成（ADR-004、AGENTS.md §3）。

> **M0-04a ～ M0-04d 已定稿（2026-09-22）**，定稿过程中的两项判定，实现时必须遵守：
>
> 1. **`NOT_COVERED` 与 `TASK_FAILED` 不是错误码，是领域状态。** 问答证据不足返回 200 + `ChatResponse.status = not_covered`；任务失败返回 200 + `Task.stage = failed` 并填 `Task.error`。做成 HTTP 4xx/5xx 会让「资料未覆盖」与「服务故障」在前端无法区分，违反 ADR-003。
> 2. **SSE 鉴权走一次性短时效令牌。** `EventSource` 不支持自定义请求头，令牌只能进查询参数，因此该令牌有效期 ≤60 秒且仅对该任务只读，不得复用常规 Bearer 令牌。
>
> 认证方案暂按 Bearer JWT（载荷含 `sub` 与 `role`）编写。该选择与 D-03 的结论无关：无论登录后端是本地演示角色表还是其他方案，传输层不变，契约不需返工。
