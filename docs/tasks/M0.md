# M0 协作与应用骨架

> 状态与列的含义见 [`docs/tasks.md`](../tasks.md)。改自己认领的那一行，**不要重排或对齐整张表**。

## 里程碑窗口

对齐参考方案 S2 表 4.2 的 **M0 需求与设计：2026-09-16 ～ 09-19**，交付物含「开发环境与仓库搭建、项目骨架」。

> **编号对照（重要）**：仓库的 M0/M1 与 S2 的 M0/M1 含义不同，不能按编号直接对应。
> 仓库 M0「协作与应用骨架」只覆盖 S2 M0 交付物里的「项目骨架 / 开发环境」一项，
> S2 M0 的其余内容（需求确认、方案初稿、系统设计）不在本仓库的任务板上。
>
> **该窗口已于 2026-09-19 关闭，今日 2026-09-22 已逾期 3 天**，而 M0 仍有 6 项未完成。
> 它们同时是 S2 M1（09-20 ～ 09-26，主链路打通）的前置，实际必须在 09-26 前补齐。

## 任务

| ID | 状态 | 任务 | 负责人 | 截止日期 | 阻塞关系 | 验收条件 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M0-01 | DONE | 建立多 Agent 协作、文档、规格、源码目录骨架 | Codex | 2026-09-19 | — | 必需文件齐全；基础校验通过 | `scripts/verify.sh`；`docs/handoffs/codex-m0-project-scaffold.md` |
| M0-02 | TODO | 初始化 Vue 3 + TypeScript + Vite 前端 | Frontend Agent | 2026-09-19（已逾期） | 阻塞 M0-04b、M1-05 | 可启动；具备最小路由、类型检查与测试命令 | 待补充 |
| M0-03 | TODO | 初始化 FastAPI 后端与健康检查 | Backend Agent | 2026-09-19（已逾期） | 阻塞 M0-04a、M1-01 | 可启动；`GET /health` 有契约和测试 | 待补充 |
| M0-04a | TODO | 起草 v1 契约真源（REST DTO、SSE 任务事件、图谱导入/导出）与 `scripts/gen-contracts.sh` | Backend Agent | 2026-09-19（已逾期） | 依赖 M0-03（导出 OpenAPI 需可运行的 FastAPI 应用）；阻塞 M0-04b、M1-01 | 三类契约在 `src/contracts/v1/python/` 齐全并符合 ADR-004～ADR-006；含 `POST /api/v1/tasks/{task_id}/cancel` 的请求/响应与错误码；`src/contracts/README.md` 的 SSE 章节改掉「`cancelled` 预留、无取消端点」的旧表述；生成物已入库；`./scripts/gen-contracts.sh --check` 通过 | 待补充 |
| M0-04b | TODO | 前端消费 v1 生成类型并回送契约反馈 | Frontend Agent | 2026-09-19（已逾期） | 依赖 M0-04a、M0-02 | `src/frontend/` 只从 `src/contracts/v1/generated/typescript/` 导入契约类型，无本地重定义或 `any` 绕过；任务状态穷尽分支含 `cancelled`；反馈写入交接文件 | 待补充 |
| M0-05 | BLOCKED | 定义 Neo4j/SQLite 开发环境与本地启动方式 | Claude（数据/后端） | 2026-09-19（已逾期） | 阻塞 M1-03（APOC）；自身被「本机无容器运行时」阻塞 | 无密钥可启动依赖；环境变量文档完整；容器内 `RETURN apoc.version()` 返回版本号 | compose、脚本与文档已交付且 `./scripts/verify.sh` 通过；**APOC 实测未完成**，见 `docs/handoffs/claude-m0-05-local-env.md` |
| M0-06 | TODO | 按 ADR-004～ADR-007 同步 `specs/course-knowledge-graph.md` | 产品/协调 Agent | 2026-09-26 | 依赖 S-01（已完成） | 验收条件 2 的状态序列与 ADR-005 一致，`cancelled` 按 ADR-006 记为正常可达终态并补上取消端点；「待细化」中失效的 M0-04 引用改为 M0-04a | 待补充 |
| M0-07 | TODO | 修正 `src/contracts/README.md` 的两处失效表述 | Backend Agent | 2026-09-26 | 依赖 S-03、S-06（均已完成）；**先于 M0-04a 做**，否则起草契约时会照抄错表述 | ① 指向已删除 `docs/decisions.md` 的引用改为 `docs/decisions/ADR-004-...md`，`grep -rn 'decisions\.md' src/` 零命中；② §4.2 状态机与「枚举一次定稿原则」按 ADR-006 改写——`cancelled` 是正常可达终态、取消端点在 MVP 实现，删掉「预留、MVP 不产生、没有取消端点、断言 worker 不会发出该状态」 | 待补充 |
| S-06 | DONE | 落地两项范围决策：实现任务取消、学习材料生成纳入加分项 | Claude（协调） | 2026-09-22 | 修订 ADR-005 第 2 条；派生 M1-06、M1-07，并扩大 M0-07 | ADR-006、ADR-007 已记录并在索引标注修订关系；product/architecture/tasks/tests/prompts 中与之矛盾的表述已全部改正；`./scripts/verify.sh` 通过 | `./scripts/verify.sh`；`docs/handoffs/claude-s06-scope-decisions.md` |
| S-01 | DONE | 确定 `src/contracts/` 的契约格式与单一真源，并拆分 M0-04 | Claude（协调） | 2026-09-22 | 阻塞 M0-04a | ADR-004、ADR-005 已记录；契约目录与命名规则可供冷启动 Agent 使用；M0-04 已拆分为顺序子任务 | `./scripts/verify.sh`；`docs/handoffs/claude-s01-contract-format.md` |
| S-03 | DONE | 拆分单文件写争用点，`verify.sh` 改造为分发器 | Claude（协调） | 2026-09-22 | — | ADR/任务/未决问题无丢失迁移；`./scripts/verify.sh` 通过且纯排版编辑不再致其失败；四个子脚本未初始化时 SKIP 且整体 exit 0 | `./scripts/verify.sh`；`docs/handoffs/claude-s03-collab-restructure.md` |

## 说明

> **M0-05 阻塞原因**：本机未安装任何容器运行时（Docker / OrbStack / Podman 均不存在），无法启动 Neo4j 实测 APOC。
> APOC 是 M1-03（`apoc.refactor.mergeNodes`）的硬前置，未实证前不得标 DONE。
> 解除方式：在装有容器运行时的机器上执行 `./scripts/dev-up.sh`，把 `RETURN apoc.version()` 的真实输出补进交接文件即可转 DONE。

> **M0-04a 与 M0-04b 顺序执行，不并行。** `src/contracts/` 只由 M0-04a 写入，`src/frontend/` 只由 M0-04b 写入；前端不得直接改契约，需求通过交接文件回送后端 Agent 改真源后重新生成（ADR-004、AGENTS.md §3）。
