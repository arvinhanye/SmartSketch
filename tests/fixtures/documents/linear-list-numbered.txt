示例数据结构讲义（虚构，仅供解析测试）

第一章 线性表

线性表是由 n 个相同类型元素组成的有限序列，n 为零时称为空表。

1.1 顺序表

顺序表用一组地址连续的存储单元依次存放线性表的元素。
按下标访问任一元素的时间复杂度为 O(1)。

1.2 单链表

单链表的每个结点包含数据域和指向后继结点的指针域。
在已知结点之后插入新结点只需修改两个指针。

第二章 树

二、基本术语

结点的度是它拥有的子树个数，树的度是树中各结点度的最大值。

三、二叉树的遍历

先序遍历按“根、左子树、右子树”的顺序访问每个结点一次。
